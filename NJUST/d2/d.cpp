#include <cstdio>
#include <cstring>
#include <algorithm>
#define ms(a,b) memset(a,b,sizeof(a))
using namespace std;
typedef long long ll;
const int maxn = 1000 + 5;

char s[maxn];
int len, nxt[maxn];

int getnxt(int i){

}

int main(){
    while (~scanf("%s", s)){
        if (s[0] == '#') break;
        len = strlen(s);
        for 
    }
    return 0;
}